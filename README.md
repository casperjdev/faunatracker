Running the app

1. Create local.properties at the root of the project (next to gradle.properties, etc.) if not already present or generated by Android Studio
2. Paste secrets given
3. In android studio, Build > Clean Project
4. Build > Assemble Project (to generate BuildConfig - values are fetched from there)
5. Run the app

